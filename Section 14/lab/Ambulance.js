class Ambulance extends Vehicle {
  
  // constructor
  constructor(color, topSpeed, direction, currentSpeed, engineStarted) {
    super(color, topSpeed, direction, currentSpeed, engineStarted);
  }
  
  // methods

}